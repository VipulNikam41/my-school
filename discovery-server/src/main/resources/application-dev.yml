server:
  port: 8761

spring:
  application:
    name: discovery-server

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
  dashboard:
    path: /eureka-web

logging:
  level:
    com:
      netflix:
        eureka: debug